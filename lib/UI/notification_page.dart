import 'package:flutter/material.dart';

class NotifiedPage extends StatelessWidget {
  const NotifiedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
